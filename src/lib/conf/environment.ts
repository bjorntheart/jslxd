/**
 * Base URL for LXD API v1
 *
 * @ignore
 */
const scheme = 'https';
const version = '1.0';
const host = '127.0.0.1';
const port = '8843';
export const API_BASE_URL = `${scheme}://${host}:${port}/${version}`;
